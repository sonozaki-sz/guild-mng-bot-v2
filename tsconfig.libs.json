{
  // VS Code 向け: pnpm 仮想ストア内の外部ライブラリ .d.ts を
  // bundler moduleResolution + ES2024 target で解析させるための専用 tsconfig。
  // tsc での使用は想定していない（noEmit: true）。
  // skipLibCheck: true により .d.ts 内部のエラーは報告されない。
  "compilerOptions": {
    "target": "ES2024",
    "module": "Preserve",
    "moduleResolution": "bundler",
    "lib": ["ES2024"],
    "skipLibCheck": true,
    "noEmit": true,
    "strict": false
  },
  "include": [
    "node_modules/.pnpm/vite@*/node_modules/vite/**/*.d.ts",
    "node_modules/.pnpm/vitest@*/node_modules/vitest/**/*.d.ts",
    "node_modules/.pnpm/@vitest+*/node_modules/@vitest/**/*.d.ts",
    "node_modules/.pnpm/rollup@*/node_modules/rollup/**/*.d.ts"
  ]
}
