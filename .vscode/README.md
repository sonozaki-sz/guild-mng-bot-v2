# VSCode 開発環境設定

このディレクトリには、ayasono の開発を効率化するVSCode設定が含まれています。

## 📁 ファイル構成

### extensions.json

プロジェクトに推奨される拡張機能のリストです。

#### 必須拡張機能

- **ESLint** - コード品質チェック
- **Prettier** - コードフォーマット
- **TypeScript拡張** - 型補完・エラー表示強化
- **Error Lens** - リアルタイムエラー表示

#### 推奨拡張機能

- **GitLens** - Git履歴可視化
- **REST Client** - API テスト（`api-tests.http`で使用）
- **Todo Tree** - TODOコメント管理
- **Path IntelliSense** - パス自動補完
- **Jest** - テストランナー
- **Prisma** - データベーススキーマ管理
- **Docker** - コンテナ管理

### settings.json

プロジェクト固有のエディター設定です。

主な設定:

- パッケージマネージャー: `pnpm`
- フォーマッター: `Prettier`
- 保存時の自動処理:
  - ESLint自動修正
  - import文の整理
  - フォーマット実行
- タブサイズ: 2スペース
- 行の長さ: 100文字

### launch.json

デバッグ設定です。以下の構成が利用可能:

1. **🤖 Bot: Debug (Dev)** - Botのみデバッグ実行
2. **🌐 Web: Debug (Dev)** - Webサーバーのみデバッグ実行
3. **🚀 All: Debug (Bot + Web)** - 両方同時にデバッグ
4. **📦 Bot: Build & Debug** - ビルド後にBot実行
5. **🔬 Test: Debug Current File** - 現在のテストファイルをデバッグ

### tasks.json

ビルド・テスト・開発タスクの定義です。

主なタスク:

- `Build: TypeScript` (Ctrl+Shift+B) - ビルド実行
- `Build: Watch` - ビルド監視モード
- `Test: Run All` - 全テスト実行
- `Dev: Start Bot` - Bot開発サーバー起動
- `Dev: Start Web` - Web開発サーバー起動
- `Lint: Check/Fix` - ESLintチェック
- `Format: Check/Write` - Prettierフォーマット

### ayasono.code-snippets

プロジェクト専用のコードスニペットです。

利用可能なスニペット:

- `discord-command` - Discordコマンドテンプレート
- `discord-event` - イベントハンドラーテンプレート
- `fastify-route` - Fastify APIルートテンプレート
- `zod-schema` - Zodバリデーションスキーマ
- `logger` - ロガーインスタンス生成
- `trylog` - try-catchブロック（ロガー付き）
- `repo-method` - リポジトリメソッドテンプレート

## 🚀 セットアップ手順

### 1. 推奨拡張機能のインストール

プロジェクトを開いた際に、VSCodeが自動的に推奨拡張機能のインストールを促します。
「すべてをインストール」をクリックしてください。

手動でインストールする場合:

1. `Ctrl+Shift+P` でコマンドパレットを開く
2. `Extensions: Show Recommended Extensions` を選択
3. 各拡張機能の「Install」をクリック

### 2. 設定の確認

設定は自動的に適用されます。確認するには:

```
設定 (Ctrl+,) → ワークスペース → 検索欄に設定名を入力
```

### 3. タスクの実行

#### ビルド

- `Ctrl+Shift+B` → デフォルトビルドタスク実行
- または `Terminal > Run Task...` から選択

#### デバッグ

1. `src/bot-main.ts` または `src/web-server.ts` を開く
2. `F5` キーでデバッグ開始
3. または左サイドバーの「実行とデバッグ」から設定を選択

### 4. コードスニペットの使用

TypeScriptファイルで以下を入力し、`Tab`キーで展開:

```typescript
discord - command; // Discord コマンドテンプレート
fastify - route; // Fastify ルートテンプレート
logger; // ロガー生成
```

### 5. REST Client の使用

1. `api-tests.http` を開く
2. `@guildId` を実際のGuild IDに変更
3. リクエストの上に表示される「Send Request」をクリック
4. レスポンスが新しいタブに表示されます

## 📝 カスタマイズ

### 個人設定の追加

プロジェクト設定を変更せず、個人的な設定を追加したい場合:

1. `settings.json` は編集せず、ユーザー設定 (`Ctrl+,`) で設定
2. または `.vscode/settings.json.local` を作成（Gitignore済み）

### スニペットの追加

`ayasono.code-snippets` に新しいスニペットを追加できます:

```json
{
  "My Custom Snippet": {
    "prefix": "my-snippet",
    "body": ["code here"],
    "description": "Description"
  }
}
```

## 🔧 トラブルシューティング

### ESLintが動作しない

1. `pnpm install` でパッケージをインストール
2. VSCodeを再起動
3. Output > ESLint でエラー確認

### フォーマットが適用されない

1. `Ctrl+Shift+P` → `Format Document` を実行
2. デフォルトフォーマッターがPrettierになっているか確認
3. `settings.json` の `editor.defaultFormatter` を確認

### デバッグが開始できない

1. `.env` ファイルが存在するか確認
2. `pnpm install` でdependenciesをインストール
3. `launch.json` の `runtimeExecutable` が `pnpm` になっているか確認

### タスクが見つからない

1. `tasks.json` のシンタックスエラーをチェック
2. `Terminal > Run Task...` でタスクリストを確認
3. VSCodeを再起動

## 📚 参考リンク

- [VSCode公式ドキュメント](https://code.visualstudio.com/docs)
- [VSCode TypeScript設定](https://code.visualstudio.com/docs/languages/typescript)
- [Debugging in VSCode](https://code.visualstudio.com/docs/editor/debugging)
- [Tasks in VSCode](https://code.visualstudio.com/docs/editor/tasks)
- [Code Snippets](https://code.visualstudio.com/docs/editor/userdefinedsnippets)
