# Bump通知機能 - 仕様書

> Disboard・ディス速のBump通知機能の詳細仕様

最終更新: 2026年2月11日

---

## 📋 概要

DiscordサーバーのBump（宣伝）コマンドを検知し、2時間後にリマインダーを送信する機能です。

### 対応サービス

| サービス名             | コマンド | Bot ID               | URL                   |
| ---------------------- | -------- | -------------------- | --------------------- |
| **Disboard**           | `/bump`  | `302050872383242240` | https://disboard.org/ |
| **ディス速 (Dissoku)** | `/up`    | 要確認               | https://dissoku.xyz/  |

---

## 🎯 主要機能

### 1. Bump検知

**messageCreateイベント**で以下を監視：

```typescript
// Disboardの場合
if (
  message.author.id === "302050872383242240" &&
  message.interaction?.commandName === "bump"
) {
  // Bump検知
}

// ディス速の場合
if (
  message.author.id === "DISSOKU_BOT_ID" &&
  message.interaction?.commandName === "up"
) {
  // Bump検知
}
```

**検知条件:**

- メッセージの送信者がBumpBot（Disboard or ディス速）
- インタラクションコマンド名が `bump` または `up`
- Bump通知機能が有効化されている

### 2. タイマー管理

**2時間後のリマインダー設定:**

```typescript
const twoHoursLater = message.createdTimestamp + 2 * 60 * 60 * 1000;

// データベースに保存
await repository.setBumpReminderDate(guildId, twoHoursLater);

// setIntervalで1秒ごとにチェック
const timerId = setInterval(async () => {
  const remindDate = await repository.getBumpReminderDate(guildId);
  if (remindDate <= Date.now()) {
    clearInterval(timerId);
    await sendReminder();
  }
}, 1000);
```

**Bot再起動時の復元:**

- クライアント起動時にデータベースから未送信のリマインダーを取得
- 各ギルドのタイマーを再設定

### 3. 通知登録UI

**ボタンコンポーネント:**

```
┌─────────────────────────────────────┐
│ 🔔 Bump通知が2時間後に送信されます │
│                                     │
│ 通知時刻: 14:30 (2時間後)          │
│                                     │
│ [🔔 通知する] [🔕 通知しない]      │
└─────────────────────────────────────┘
```

**機能:**

- `🔔 通知する`: ユーザーを通知リストに追加
- `🔕 通知しない`: ユーザーを通知リストから削除
- ボタンは2時間で無効化

### 4. リマインダー送信

**通知対象:**

1. 設定されたメンションロール
2. 通知ボタンで登録した個別ユーザー

**通知メッセージ例:**

```
@BumpRole @User1 @User2 @User3

⏰ Bumpの時間です！
次のBumpまであと2時間です。
```

---

## ⚙️ 設定コマンド

### `/bump-reminder-config`

#### サブコマンド

**1. `start` - 機能有効化**

```
/bump-reminder-config start
```

- Bump通知機能を有効化
- メンション設定はリセット

**2. `stop` - 機能無効化**

```
/bump-reminder-config stop
```

- Bump通知機能を無効化
- 進行中のタイマーも停止

**3. `set-mention` - メンションロール設定**

```
/bump-reminder-config set-mention role:@BumpRole
/bump-reminder-config set-mention  # ロール省略で無効化
```

- リマインダー送信時にメンションするロールを設定
- ロールは「メンション可能」である必要がある

**4. `show` - 設定状態表示**

```
/bump-reminder-config show
```

**表示内容:**

```
┌─────────────────────────────┐
│ Bump通知                    │
├─────────────────────────────┤
│ 状態: 実行中                │
│ メンションロール: @BumpRole │
│ 通知登録ユーザー: @User1, @User2 │
└─────────────────────────────┘
```

---

## 💾 データベーススキーマ

### GuildConfig

```prisma
model GuildConfig {
  // ...
  bumpReminderConfig String? // JSON: BumpReminderConfig
}
```

### BumpReminderConfig 型定義

```typescript
interface BumpReminderConfig {
  enabled: boolean; // 機能有効/無効
  channelId?: string; // リマインダー送信チャンネルID（Bot再起動時の復元に必須）
  mentionRoleId?: string; // メンションロールID
  mentionUserIds?: string[]; // 通知登録ユーザーIDリスト
  nextRemindDate?: number; // 次回通知日時（Unix timestamp）
}
```

**重要**: `channelId`と`nextRemindDate`はBot再起動時の復元に必須のため、タイマー設定時に必ず保存すること。

---

## 🔄 処理フロー

### Bump検知から通知まで

```
1. ユーザーが /bump または /up を実行
   ↓
2. Disboard/ディス速がBump成功メッセージを送信
   ↓
3. messageCreateイベントで検知
   - Bot IDとコマンド名を確認
   - 機能有効化を確認
   ↓
4. タイマー設定 + データベース保存
   - 2時間後の日時を計算
   - チャンネルIDと通知日時をDBに保存（重要！）
   - setIntervalでタイマー開始
   ↓
5. 通知登録UI表示
   - Embed + ボタンコンポーネント
   - ボタンクリックでユーザー登録/解除
   - 登録/解除の度にDBを更新
   ↓
6. 2時間経過（またはBot再起動後の復元）
   - タイマー発火
   - メンションロール+登録ユーザーに通知
   - データベースから削除（nextRemindDateとchannelIdをクリア）
```

**データベース保存タイミング:**

- Bump検知時: `nextRemindDate`、`channelId`を保存
- ボタンクリック時: `mentionUserIds`を更新
- 通知送信後: `nextRemindDate`、`channelId`、`mentionUserIds`をクリア

### Bot再起動時の復元

**重要**: Bot停止中もリマインダーが継続できるよう、すべての状態をデータベースに永続化

```
1. Bot起動（clientReady）
   ↓
2. 全ギルドのbumpReminderConfigを取得
   ↓
3. nextRemindDateが設定されているギルドを抽出
   ↓
4. 各ギルドでタイマー再設定
   - すでに通知時刻を過ぎている場合
     → 即時リマインダー送信
     → データベースから削除
   - 未来の場合
     → 残り時間でsetIntervalタイマー設定
     → 通知UIボタンは再表示しない（期限切れのため）
```

**実装のポイント:**

```typescript
// clientReadyイベントで実行
async function restoreBumpReminders() {
  const guilds = client.guilds.cache;

  for (const [guildId, guild] of guilds) {
    const config = await repository.getBumpReminderConfig(guildId);

    if (!config?.enabled || !config.nextRemindDate || !config.channelId) {
      continue;
    }

    const now = Date.now();
    const remindDate = config.nextRemindDate;

    if (remindDate <= now) {
      // すでに通知時刻を過ぎている → 即時送信
      await sendReminder(guildId, config.channelId, config);
      await repository.clearBumpReminder(guildId);
    } else {
      // 未来 → タイマー再設定
      const remainingTime = remindDate - now;
      startReminderTimer(guildId, config.channelId, remainingTime, config);
    }
  }
}
```

**保存必須データ:**

- `nextRemindDate`: 通知予定日時（Unix timestamp）
- `channelId`: リマインダーを送信するチャンネルID
- `mentionRoleId`: メンションロールID（オプション）
- `mentionUserIds`: 通知登録ユーザーIDリスト

---

## 🛡️ エラーハンドリング

### 想定されるエラー

1. **チャンネルが削除された**
   - リマインダー送信をスキップ
   - ログに記録

2. **ロールが削除された**
   - ロールメンションをスキップ
   - ユーザーメンションのみ送信

3. **ユーザーがサーバーを退出**
   - そのユーザーのメンションをスキップ
   - 他のユーザーには通知

4. **Bot権限不足**
   - DMでエラー通知（可能なら）
   - ログに記録

5. **Bot再起動時の復元エラー**
   - チャンネルが削除されている
     → リマインダーをスキップ、DBから削除
   - ギルドから退出済み
     → データのクリーンアップ
   - データベース接続エラー
     → リトライ後、失敗したらログに記録

---

## 🧪 テストケース

### ユニットテスト

- [ ] Disboard Bump検知
- [ ] ディス速 Up検知
- [ ] タイマー計算（2時間後）
- [ ] メンションテキスト生成
- [ ] 設定コマンド各種
- [ ] Bot再起動時の復元ロジック

### インテグレーションテスト

- [ ] messageCreateイベントフロー全体
- [ ] ボタンクリック処理
- [ ] タイマー発火とリマインダー送信
- [ ] Bot再起動時の復元
  - [ ] 通知時刻前の復元（タイマー再設定）
  - [ ] 通知時刻後の復元（即時送信）
  - [ ] チャンネル削除時の復元
- [ ] データベース永続化の確認

---

## 📝 実装メモ

### ディス速のBot ID確認方法

1. ディス速をサーバーに招待
2. `/up` コマンドを実行
3. レスポンスメッセージの送信者IDを確認
4. 確認したIDを定数に設定:

```typescript
const DISSOKU_BOT_ID = "確認したID";
```

### Bot再起動時のタイマー復元

**重要な考慮事項:**

1. **データベース永続化が必須**
   - setIntervalはメモリ上のタイマーなので、Bot停止で消失
   - すべての状態をDBに保存することで、いつでも復元可能

2. **clientReadyイベントで復元処理を実行**
   - Bot起動時に一度だけ実行
   - すべてのギルドをスキャン

3. **タイマーIDの管理**

   ```typescript
   // ギルドごとのタイマーIDを保持（メモリ上）
   const reminderTimers = new Map<string, NodeJS.Timeout>();

   // 新規タイマー設定時
   function startReminderTimer(guildId: string, ...) {
     // 既存タイマーがあればクリア
     const existingTimer = reminderTimers.get(guildId);
     if (existingTimer) {
       clearInterval(existingTimer);
     }

     // 新しいタイマーを設定
     const timerId = setInterval(...);
     reminderTimers.set(guildId, timerId);
   }
   ```

4. **過去の通知時刻の処理**
   - Bot停止中に通知時刻を過ぎた場合でも、復元時に即座に通知
   - ユーザーの期待を裏切らない

### 重複防止機構

- 同じギルドで複数人が短時間にBumpした場合
- 最後のBumpを基準にタイマーをリセット
- 古いタイマーはクリア

---

## 🔗 参考リソース

- [Disboard公式サイト](https://disboard.org/)
- [ディス速公式サイト](https://dissoku.xyz/)
- [Discord.js - MessageCreateEvent](https://discord.js.org/#/docs/discord.js/main/class/Client?scrollTo=e-messageCreate)
- [Discord.js - ButtonBuilder](https://discord.js.org/#/docs/discord.js/main/class/ButtonBuilder)

---

**次のステップ**:

1. ディス速のBot IDを確認
2. データベーススキーマにchannelIdを追加
3. Bot再起動時の復元処理を実装（clientReadyイベント）
4. タイマー管理とデータベース永続化を実装
