# VAC（ボイスチャンネル自動作成）機能 - 仕様書

> Voice Auto Create - 専用ボイスチャンネル自動作成機能

最終更新: 2026年2月11日

---

## 📋 概要

ユーザーがトリガーチャンネル（CreateVC）に参加すると、自動的にそのユーザー専用のボイスチャンネルを作成する機能です。

**VC管理**: 作成されたボイスチャンネルは、VCのチャットチャンネルに設置される操作パネルからVC名や人数制限を変更できます。さらに、作成者には`ManageChannels`権限が付与されるため、Discord標準のチャンネル設定からもより詳細な設定変更が可能です（ビットレート、地域など）。

---

## 🎯 主要機能

### 1. 自動チャンネル作成

**トリガー**: 指定されたトリガーチャンネルに入室

**作成されるチャンネル:**

- **ボイスチャンネル**: `{ユーザー名}'s Room`

**備考**: テキストチャンネルは別途作成せず、VCに紐づくチャットチャンネルを利用します。操作パネルはこのチャットチャンネルに設置されます。

**処理フロー:**

```
1. ユーザーがトリガーチャンネル（CreateVC）に参加
   ↓
2. voiceStateUpdateイベントで検知
   ↓
3. 新しいVCを作成
   - 名前: {ユーザー名}'s Room
   - カテゴリ: トリガーチャンネルと同じ
   - 人数制限: 99（デフォルト）
   - 作成者にManageChannels権限を付与
   ↓
4. VCのチャットチャンネルに操作パネルを設置
   ↓
5. ユーザーを新しいVCに自動移動
   ↓
6. データベースに作成したVCのIDを保存
```

---

## 🎛️ 操作パネル機能

VCのチャットチャンネルに配置される操作パネル

**配置場所**: 作成されたボイスチャンネルのチャット

### パネルUI設計

```
┌──────────────────────────────────┐
│ 🎤 ボイスチャンネル操作パネル    │
├──────────────────────────────────┤
│ このパネルからVCの設定を変更で   │
│ きます。                         │
│                                  │
│ [✏️ VC名を変更]                  │
│ [👥 人数制限を変更]              │
│ [🔇 メンバーをAFKに移動]         │
│ [🔄 パネルを最下部に移動]        │
└──────────────────────────────────┘
```

### ボタン機能

**✏️ VC名を変更**

- Modalを表示してVC名を入力
- 入力されたVC名でボイスチャンネル名を変更
- 変更完了後、確認メッセージを表示

**実装詳細:**

```typescript
// ボタンクリック時
1. Text Input Modalを表示
   - 現在のVC名をデフォルト値として設定
2. ユーザーが新しいVC名を入力
3. VCの名前を変更
4. 結果をEmbedで通知
```

**👥 人数制限を変更**

- Modalを表示して人数制限を入力（0-99）
- 入力された数値でボイスチャンネルの人数制限を変更
- 0を入力すると無制限に設定
- 変更完了後、確認メッセージを表示

**実装詳細:**

```typescript
// ボタンクリック時
1. Text Input Modalを表示
   - 現在の人数制限をデフォルト値として設定
   - プレースホルダー: "0-99 (0=無制限)"
2. ユーザーが人数制限を入力
3. バリデーション（0-99の範囲チェック）
4. VCの人数制限を変更
5. 結果をEmbedで通知
```

**🔇 メンバーをAFKに移動**

- User Select Menuを表示
- VCに現在参加しているメンバーから選択可能（複数選択可）
- 選択されたメンバーをAFKチャンネルに移動
- 移動完了後、確認メッセージ（移動したメンバー名を表示）

**実装詳細:**

```typescript
// ボタンクリック時
1. VCに参加中のメンバーリストを取得
2. User Select Menuを動的に生成
3. ユーザーが移動対象を選択（複数可）
4. 選択されたメンバーをAFKチャンネルに移動
5. 結果をEmbedで通知
```

**🔄 パネルを最下部に移動**

- 既存のパネルメッセージを削除
- チャンネルの最下部に新しいパネルを再送信
- チャットが流れてパネルが見つけにくくなった時に使用

**実装詳細:**

```typescript
// ボタンクリック時
1. 現在のパネルメッセージを削除
2. 同じ内容のパネルを新規作成して送信
3. データベースに新しいパネルメッセージIDを保存（必要に応じて）
```

### その他の設定変更

**Discord標準のチャンネル設定から変更:**

- VAC作成時に作成者に`ManageChannels`権限を付与
- 作成者はDiscordの標準UIでより詳細なチャンネル設定を変更可能
  - チャンネルを右クリック → 「チャンネルを編集」
  - ビットレートの変更
  - 地域の変更
  - 権限の詳細設定など

---

## ⚙️ 設定コマンド

### `/vac-config`

#### サブコマンド

**1. `create-trigger` - トリガーチャンネル作成**

```
/vac-config create-trigger
```

- コマンド実行したチャンネルと同じカテゴリーに「CreateVC」という名前のボイスチャンネルを自動作成
- 作成したチャンネルを自動的にトリガーチャンネルとして登録
- データベースにトリガーチャンネルIDを保存

**処理フロー:**

```typescript
1. コマンド実行チャンネルのカテゴリーを取得
2. 「Create VC」という名前のボイスチャンネルを作成
3. データベースのtriggerChannelIdsに追加
4. 確認メッセージを送信
```

**注意事項:**

- トリガーチャンネルが削除された場合、`channelDelete`イベントで自動的に登録を解除
- 同じカテゴリー内に「CreateVC」が既に存在する場合はエラー

**2. `remove-trigger` - トリガーチャンネル削除**

```
/vac-config remove-trigger channel:#CreateVC
```

- 指定したトリガーチャンネルを削除
- データベースからトリガーチャンネル登録を解除
- チャンネルが存在しない場合でも、データベースから削除

**処理フロー:**

```typescript
1. 指定されたチャンネルがトリガーチャンネルかチェック
2. チャンネルが存在する場合は削除
3. データベースのtriggerChannelIdsから削除
4. 確認メッセージを送信
```

**3. `show` - 現在の設定表示**

```
/vac-config show
```

---

## 🗑️ 自動削除機能

**削除条件**: VCが完全に空になったとき

**処理フロー:**

```
1. voiceStateUpdateイベントで退出を検知
   ↓
2. VCが空かチェック
   ↓
3. 空の場合
   - VCを削除
   - データベースから削除
```

---

## 💾 データベーススキーマ

### GuildConfig

```prisma
model GuildConfig {
  // ...
  vacConfig String? // JSON: VacConfig
}
```

### VacConfig 型定義

```typescript
interface VacConfig {
  enabled: boolean; // 機能有効/無効
  triggerChannelIds: string[]; // トリガーチャンネルIDリスト
  createdChannels: VacChannelPair[]; // 作成されたチャンネル一覧
}

interface VacChannelPair {
  voiceChannelId: string; // ボイスチャンネルID
  ownerId: string; // 作成者（所有者）のユーザーID
  createdAt: number; // 作成日時（Unix timestamp）
}
```

---

## 🔄 処理フロー詳細

### VC作成処理

```typescript
async function createVoiceChannel(
  member: GuildMember,
  triggerChannel: VoiceChannel,
) {
  // 1. VCを作成
  const voiceChannel = await guild.channels.create({
    name: `${member.displayName}'s Room`,
    type: ChannelType.GuildVoice,
    parent: triggerChannel.parent,
    userLimit: 99,
    permissionOverwrites: [
      {
        id: member.id,
        allow: [PermissionFlagsBits.ManageChannels],
      },
    ],
  });

  // 2. VCのチャットチャンネルに操作パネルを設置
  const chatChannel = voiceChannel; // VCに紐付いたチャット
  await sendControlPanel(chatChannel);

  // 3. ユーザーを移動
  await member.voice.setChannel(voiceChannel);

  // 4. データベースに保存
  await saveCreatedChannel(voiceChannel.id, member.id);
}
```

---

## 🛡️ エラーハンドリング

### 想定されるエラー

1. **権限不足**
   - チャンネル作成権限がない
   - ユーザー移動権限がない
     → エラーメッセージ送信、ログ記録

2. **カテゴリが満杯**
   - カテゴリ内のチャンネル数が上限（50）に達している
     → 別のカテゴリに作成 or エラー通知

3. **名前の重複**
   - 同じ名前のチャンネルが既に存在
     → 数字サフィックスを追加（例: `User's Room (2)`）

4. **Bot再起動時のクリーンアップ**
   - 空のVCが残っている場合は削除
   - データベースと実際のチャンネルの同期

5. **トリガーチャンネル削除検知**
   - `channelDelete`イベントでトリガーチャンネルの削除を検知
   - データベースのtriggerChannelIdsから削除されたチャンネルIDを除去
   - ログに記録

**実装例:**

```typescript
// channelDeleteイベント
async function handleChannelDelete(channel: GuildChannel) {
  const config = await repository.getVacConfig(channel.guildId);
  if (config.triggerChannelIds.includes(channel.id)) {
    // トリガーチャンネルリストから削除
    config.triggerChannelIds = config.triggerChannelIds.filter(
      (id) => id !== channel.id,
    );
    await repository.updateVacConfig(channel.guildId, config);
    logger.info(`[VAC] Trigger channel deleted: ${channel.name}`);
  }
}
```

---

## 🧪 テストケース

### ユニットテスト

- [ ] トリガーチャンネル判定
- [ ] トリガーチャンネル作成ロジック
- [ ] トリガーチャンネル削除検知
- [ ] VC名生成ロジック
- [ ] 権限設定（ManageChannels付与）
- [ ] 削除条件判定

### インテグレーションテスト

- [ ] トリガーチャンネル作成フロー
- [ ] トリガーチャンネル削除時の自動登録解除
- [ ] VC作成フロー全体
- [ ] 作成者のManageChannels権限確認
- [ ] 操作パネルボタン動作（VC名変更）
- [ ] 操作パネルボタン動作（人数制限変更）
- [ ] 操作パネルボタン動作（AFK移動）
- [ ] 自動削除処理
- [ ] Bot再起動時のクリーンアップ

---

## 📚 参考リソース

- [Discord.js - VoiceStateUpdate](https://discord.js.org/#/docs/discord.js/main/class/Client?scrollTo=e-voiceStateUpdate)
- [Discord.js - ChannelDelete](https://discord.js.org/#/docs/discord.js/main/class/Client?scrollTo=e-channelDelete)
- [Discord.js - Channels](https://discord.js.org/#/docs/discord.js/main/class/GuildChannelManager)
- [Discord.js - Permissions](https://discord.js.org/#/docs/discord.js/main/class/PermissionsBitField)
- [Discord.js - UserSelect](https://discord.js.org/#/docs/discord.js/main/class/UserSelectMenuBuilder)
- [Discord.js - Modal](https://discord.js.org/#/docs/discord.js/main/class/ModalBuilder)
- [Discord.js - TextInput](https://discord.js.org/#/docs/discord.js/main/class/TextInputBuilder)

---

**次のステップ**:

1. トリガーチャンネル自動作成機能の実装
2. channelDeleteイベントでの登録解除処理
3. ManageChannels権限付与の実装
4. 操作パネルの実装
   - VC名変更機能（Modal）
   - 人数制限変更機能（Modal）
   - AFK移動機能（User Select Menu）
